# CAM: CAD-модель → G-Code — зависимости Python
# Установка: pip install -r requirements.txt
# FreeCAD ставится отдельно (AppImage), см. README.md

# YAML-конфиг
pyyaml>=6.0

# Верификация результата обработки (verify.py) — сравнение меша с эталоном.
# Ядру пайплайна (run_cam.py) не нужны; ставятся ради verify.py.
numpy>=1.24
scipy>=1.10
trimesh>=4.0
