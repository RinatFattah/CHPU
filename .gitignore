# Python
__pycache__/
*.py[cod]
*.pyo
*.pyd

# Виртуальные окружения
venv/
.venv/
env/
.env/

# Конфиг с секретами (шаблон — config.example.yaml — коммитится)
config.yaml
config.local.yaml
*.local.yaml

# Переменные окружения
.env
.env.*

# Выходные файлы pipeline
*.stl
*.gcode
*_stock.stp
*_sim.stp
# Артефакты симуляции NX (рабочие файлы)
*_ipw.prt
*_sim.prt
*.mpf

# IDE
.idea/
.vscode/
*.swp
*.swo

# OS
.DS_Store
Thumbs.db
desktop.ini
*_stock.step
*_part.step
*_autofix.json

# Прогоны и материалы к показам — рабочие выхлопы, в гит не идут.
# runs/  — прогоны пайплайна (в т.ч. runs/web/ от веб-морды)
# demo/  — то, что собрано для созвонов: модели, программы, результаты
runs/
demo/

# logs
*.log

# Ключи доступа: НИКОГДА не коммитить
.gigachat_key
.openrouter_key
